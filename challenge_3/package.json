{
  "name": "multistep_checkout",
  "version": "1.0.0",
  "description": "Series of forms to simulate a shopping cart checkout experience.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "build": "npx babel client/ --out-dir public --presets=@babel/preset-react --ignore=node_modules,compiled --source-maps inline",
    "watch": "npx babel client/ --out-dir public --presets=@babel/preset-react --ignore=node_modules,compiled --source-maps inline --watch"
  },
  "author": "Adam Brent",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "mysql": "^2.18.1",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "devDependencies": {
    "@babel/cli": "^7.12.10",
    "@babel/core": "^7.12.10",
    "@babel/preset-env": "^7.12.11",
    "@babel/preset-react": "^7.12.10",
    "nodemon": "^2.0.7"
  }
}
